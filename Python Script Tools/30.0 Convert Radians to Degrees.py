import math

def radianes_a_grados(radianes)
grados = radianes * (180/math.pi)

	return grados 

print(radianes_a_grados(math.pi))
print(radianes_a_grados(math.pi/2))
print(radianes_a_grados(math.pi/3))